# 이 파일을 .env 로 복사해서 사용하세요. (.env 는 git 에 올리지 않습니다)
# 데이터 모드 전환용 설정입니다.

# ── 프론트 단독 시연(mock) 모드 — 기본값 ──
VITE_USE_MOCK_API=true
VITE_API_BASE_URL=

# ── 백엔드(WMS_back) 연동 시 ──
# VITE_USE_MOCK_API=false
# VITE_API_BASE_URL=http://localhost:18080/api
